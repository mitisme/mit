
public enum ID {
    //Defines what kind of objects these are
    //Makes sure everything isn't the same type of thing
   Player(),
   Meteor(),
   Block(), 
   Crate(),
   Bullet(),
   Enemy();
}